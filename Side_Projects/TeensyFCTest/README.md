# TeensyFCTest

Minimal hardware test harness for Teensy boards.

Current scope:

- USB serial menu
- I2C bus scanner on the default `Wire` bus

This project intentionally starts small so new board bring-up can happen
without pulling in the full flight software stack.
